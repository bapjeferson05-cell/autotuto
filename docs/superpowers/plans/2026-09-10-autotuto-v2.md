# AutoTuto v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reescrever o AutoTuto do zero — um professor de matemática por IA que explica, desenha e conversa com interrupção real — sem o Frankenstein do v1.

**Architecture:** Módulos com uma responsabilidade cada e dependências que só apontam pra baixo. `schema`/`calc`/`figuras`/`estado` não sabem que existe LLM ou voz. `tocador` é o único orquestrador. Interrupção em 3 camadas: regex → LLM curto → fallback honesto. Voz é um adapter fino próprio (Piper + faster-whisper direto), não o jarvis.

**Tech Stack:** Python 3.13, matplotlib + numpy + pillow, piper-tts, faster-whisper, `http.server` da stdlib, `urllib` pro LLM (Ollama local ou Anthropic API). Sem framework web, sem SDK de LLM, sem torch explícito.

**Spec:** `docs/SPEC.md`

## Global Constraints

- **Python 3.13**, um venv, `pyproject.toml`. Deps exatas: `matplotlib`, `numpy`, `pillow`, `piper-tts`, `faster-whisper`. Nada além disso sem justificativa no PR.
- **Nunca mentir pro aluno.** O professor responde de verdade ou admite que não preparou. Proibido: forçar gatilho inexistente; frase "deixa eu achar um jeito melhor" sem ação.
- **Todo knob (timing, modelo, porta, cor) vive em `autotuto/config.py`.** Nenhum número mágico solto noutro arquivo.
- **`diz` (fala do professor) nunca contém LaTeX** nem `\frac`. LaTeX só em `calc.passos` e `figuras/lousa.passo_latex`.
- **PT-BR** em toda string voltada ao aluno.
- **Dependência só aponta pra baixo** (ver ordem em File Structure). `schema`, `calc`, `figuras`, `estado`, `classificador` não importam `llm`, `cerebro`, `voz`, `tocador`.
- **Visor:** só `http.server` da stdlib. Zero dep de frontend, zero JS externo.
- **LLM:** HTTP puro via `urllib`. Uma função pública: `llm.perguntar(mensagens, timeout) -> str`.
- **Determinismo pro vídeo:** `demo_roteiro.py` roda com `cerebro=None` e aula de ouro — sem STT, sem LLM.
- **Commits frequentes:** um commit por passo verde. Mensagens em PT-BR, imperativo.
- Trailer em todo commit:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01TtQc7aQ6h17sxMqQj9ZgiR
  ```

---

## File Structure

```
pyproject.toml              # deps + pytest config
autotuto/
  __init__.py
  config.py                 # TODOS os knobs. Não importa nada do pacote.
  schema.py                 # Aula/Beat/Ramo dataclasses + validar_estrutura(dict) -> list[str]
  calc.py                   # funções puras -> Resultado(valor, passos, unidade); CATALOGO
  figuras/
    __init__.py
    lousa.py                # tema (lê config) + passo_latex(latex) -> bytes
    canvas.py               # figura(spec) -> bytes  (pontos/segmentos/polígonos/ângulos/rótulos)
    catalogo.py             # geradores nomeados usados pelas aulas: trapezio, triangulo, ... -> spec
  estado.py                 # EstadoAula + Trilha (pilha)
  aulas.py                  # 4 aulas de ouro (dict) + RAMOS_GENERICOS + carregar/disponiveis
  classificador.py          # classificar(fala, ramos) -> str | None
  llm.py                    # perguntar(mensagens, *, timeout) -> str   (ollama | claude)
  cerebro.py                # roteia_interrupcao(fala, contexto, ramos) -> str | None
  validador.py              # checar_matematica(aula_dict) -> list[str]
  planejador.py             # planeja(problema) -> (Aula, Relatorio)
  tocador.py                # Tocador.toca(aula, *, interrupcoes=None, respostas=None) -> EstadoAula
  visor.py                  # Visor (http.server): falar/ouvir/desenhar + teclado/texto
  voz.py                    # Voz: Piper + faster-whisper direto. falar/ouvir. pré-aquece.
demos/
  demo_texto.py
  demo_voz.py
  demo_roteiro.py
roteiros/
  trapezio.json
tests/
  test_schema.py test_calc.py test_figuras.py test_estado.py test_aulas.py
  test_classificador.py test_llm.py test_cerebro.py test_validador.py
  test_planejador.py test_tocador.py test_visor.py
docs/SPEC.md
```

**Dependency direction (imports allowed):**
- `config` → (nada)
- `schema` → `config`
- `calc` → `config`
- `figuras/*` → `config`
- `estado` → `schema`
- `classificador` → `config`
- `llm` → `config`
- `cerebro` → `config`, `llm`, `classificador`
- `validador` → `calc`, `figuras`
- `aulas` → `schema`
- `planejador` → `llm`, `schema`, `validador`, `aulas`
- `tocador` → `estado`, `classificador`, `cerebro`, `figuras`, `calc`, `config`
- `visor` → `config`
- `voz` → `config`
- `demos/*` → tudo

---

## Task 0: Zerar o repo e montar o esqueleto

**Files:**
- Create: `pyproject.toml`, `autotuto/__init__.py`, `autotuto/figuras/__init__.py`, `tests/__init__.py`, `.gitignore`, `README.md`

**Interfaces:**
- Produces: pacote `autotuto` importável; `pytest` roda (0 testes); venv `.venv` com as 5 deps.

- [ ] **Step 1: Preservar os docs e resetar o histórico**

```bash
cd ~/professor-matematica
git bundle create ../autotuto-v1-arquivo.bundle --all      # backup do v1 inteiro
mkdir -p /tmp/keep && cp -r docs /tmp/keep/                  # SPEC + este plano
git checkout --orphan v2 && git rm -rf . >/dev/null
rm -rf professor demos roteiros *.py *.md out 2>/dev/null || true
cp -r /tmp/keep/docs .
```

- [ ] **Step 2: Criar `pyproject.toml`**

```toml
[project]
name = "autotuto"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = ["matplotlib", "numpy", "pillow", "piper-tts", "faster-whisper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
```

- [ ] **Step 3: Criar `.gitignore`**

```
.venv/
__pycache__/
*.pyc
out/
models/
```

- [ ] **Step 4: Criar os `__init__.py` vazios e um `README.md` de uma linha**

`autotuto/__init__.py`, `autotuto/figuras/__init__.py`, `tests/__init__.py` — vazios.
`README.md`: `# AutoTuto — professor de matemática interativo. Ver docs/SPEC.md.`

- [ ] **Step 5: venv + deps + pytest roda**

```bash
python3 -m venv .venv
.venv/bin/pip install -e . pytest
.venv/bin/pytest        # "no tests ran" — ok
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "esqueleto do v2 (repo zerado, docs preservados)"
git push -f origin v2:main      # sobrescreve o main com o v2
git branch -M main
```

---

## Task 1: config.py + schema.py

**Files:**
- Create: `autotuto/config.py`, `autotuto/schema.py`, `tests/test_schema.py`

**Interfaces:**
- Produces:
  - `config` com todas as constantes da §8 do SPEC (nomes exatos: `CEREBRO_TIMEOUT_S`, `FALA_TIMEOUT_S`, `GRAVA_RESTO_S`, `SILENCIO_MS`, `STT_MODELO`, `BARGE_IN`, `SETTLE_S`, `RITMO_S_POR_CHAR`, `PAUSA`, `VISOR_PORTA`, `VISOR_POLL_MS`, `LLM_PROVEDOR`, `LLM_MODELO`, `LLM_CLAUDE`, `PLANEJADOR_TIMEOUT_S`, `TTS_LENGTH_SCALE`, cores `COR_*`).
  - `schema.Aula` (dataclass: `titulo:str, blocos:list[dict], ramos:dict[str,list[dict]], topico:str="", dados:dict=field(...)`), com `de_json(dict)->Aula` e `para_json()->dict`.
  - `schema.validar_estrutura(obj: dict) -> list[str]` — lista de erros (vazia = ok).

- [ ] **Step 1: Escrever `config.py`** (copiar a §8 do SPEC verbatim; `env = lambda k, d: os.environ.get(k, d)`)

- [ ] **Step 2: Escrever os testes de schema (falhando)**

```python
# tests/test_schema.py
from autotuto.schema import Aula, validar_estrutura

AULA_OK = {"titulo": "T", "topico": "area_trapezio", "dados": {"B": 18},
           "blocos": [{"diz": "olá"}],
           "ramos": {"por_que": [{"diz": "porque sim"}]}}

def test_aula_valida_nao_tem_erros():
    assert validar_estrutura(AULA_OK) == []

def test_bloco_sem_diz_figura_calc_e_erro():
    obj = {**AULA_OK, "blocos": [{}]}
    assert any("vazio" in e for e in validar_estrutura(obj))

def test_pergunta_precisa_de_senao():
    obj = {**AULA_OK, "blocos": [{"diz": "x", "pergunta": {"escuta_s": 10}}]}
    assert any("senao" in e for e in validar_estrutura(obj))

def test_pergunta_senao_tem_que_ser_ramo_existente():
    obj = {**AULA_OK, "blocos": [{"diz": "x", "pergunta": {"escuta_s": 10, "senao": "fantasma"}}]}
    assert any("fantasma" in e for e in validar_estrutura(obj))

def test_acerta_tem_que_ser_lista_de_strings():
    obj = {**AULA_OK, "blocos": [{"diz": "x", "pergunta":
           {"escuta_s": 10, "senao": "por_que", "acerta": "triangulo"}}]}
    assert any("acerta" in e for e in validar_estrutura(obj))

def test_de_json_e_para_json_roundtrip():
    a = Aula.de_json(AULA_OK)
    assert a.para_json()["titulo"] == "T"
    assert a.ramos["por_que"][0]["diz"] == "porque sim"
```

- [ ] **Step 3: Rodar — falha**

Run: `.venv/bin/pytest tests/test_schema.py -v` → FAIL (`ModuleNotFoundError`)

- [ ] **Step 4: Implementar `schema.py`**

```python
from __future__ import annotations
from dataclasses import dataclass, field

_ESPERAS = {"curta", "media", "longa"}

@dataclass
class Aula:
    titulo: str
    blocos: list[dict]
    ramos: dict[str, list[dict]] = field(default_factory=dict)
    topico: str = ""
    dados: dict = field(default_factory=dict)

    @classmethod
    def de_json(cls, o: dict) -> "Aula":
        return cls(o.get("titulo", "Aula"), o.get("blocos", []), o.get("ramos", {}),
                   o.get("topico", ""), o.get("dados", {}))

    def para_json(self) -> dict:
        return {"titulo": self.titulo, "topico": self.topico, "dados": self.dados,
                "blocos": self.blocos, "ramos": self.ramos}


def validar_estrutura(o: dict) -> list[str]:
    erros: list[str] = []
    if not isinstance(o.get("blocos"), list) or not o["blocos"]:
        erros.append("aula sem 'blocos'")
        return erros
    ramos = o.get("ramos", {})
    if not isinstance(ramos, dict):
        erros.append("'ramos' tem que ser um objeto")
        ramos = {}
    for i, b in enumerate(o["blocos"]):
        onde = f"bloco[{i}]"
        if not (b.get("diz") or b.get("figura") or b.get("calc")):
            erros.append(f"{onde}: vazio (precisa de diz, figura ou calc)")
        pg = b.get("pergunta")
        if pg is not None:
            if not isinstance(pg, dict):
                erros.append(f"{onde}.pergunta: tem que ser objeto")
                continue
            if not pg.get("senao"):
                erros.append(f"{onde}.pergunta: falta 'senao'")
            elif pg["senao"] not in ramos:
                erros.append(f"{onde}.pergunta.senao='{pg['senao']}' não é um ramo")
            es = pg.get("escuta_s", 12)
            if not isinstance(es, (int, float)) or not (3 <= es <= 60):
                erros.append(f"{onde}.pergunta.escuta_s: 3 a 60")
            ac = pg.get("acerta")
            if ac is not None and (not isinstance(ac, list) or not all(isinstance(x, str) for x in ac)):
                erros.append(f"{onde}.pergunta.acerta: lista de textos ou omita")
            cf = pg.get("confirma")
            if cf is not None and not isinstance(cf, str):
                erros.append(f"{onde}.pergunta.confirma: texto ou omita")
        esp = b.get("espera")
        if esp is not None and esp not in _ESPERAS:
            erros.append(f"{onde}.espera: {sorted(_ESPERAS)} ou omita")
    for nome, blocos in ramos.items():
        if not isinstance(blocos, list) or not blocos:
            erros.append(f"ramo '{nome}': lista não-vazia de beats")
    return erros
```

- [ ] **Step 5: Rodar — passa**

Run: `.venv/bin/pytest tests/test_schema.py -v` → PASS (6)

- [ ] **Step 6: Commit**

```bash
git add autotuto/config.py autotuto/schema.py tests/test_schema.py
git commit -m "config (todos os knobs) + schema (Aula + validar_estrutura)"
```

---

## Task 2: calc.py

**Files:**
- Create: `autotuto/calc.py`, `tests/test_calc.py`

**Interfaces:**
- Consumes: nada (só `config` pra `import`, opcional).
- Produces:
  - `calc.Resultado` (dataclass: `valor: float|str|tuple`, `passos: list[str]` (LaTeX), `unidade: str = ""`).
  - Funções puras: `area_trapezio(B, b, h)`, `area_triangulo(base, altura)`, `area_retangulo(base, altura)`, `pitagoras(a=None, b=None, c=None)`, `eq_primeiro_grau(a, b)`, `regra_de_tres(a, b, c)`. Todas `-> Resultado`.
  - `calc.CATALOGO: dict[str, callable]` com as 6.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_calc.py
import math
from autotuto.calc import (Resultado, area_trapezio, area_triangulo, area_retangulo,
                           pitagoras, eq_primeiro_grau, regra_de_tres, CATALOGO)

def test_trapezio_valor_e_passos():
    r = area_trapezio(B=18, b=10, h=6)
    assert r.valor == 84 and r.unidade == "u²"
    assert r.passos[0].startswith("A =") and "84" in r.passos[-1]

def test_triangulo():
    assert area_triangulo(8, 5).valor == 20

def test_pitagoras_hipotenusa():
    assert round(pitagoras(a=3, b=4).valor, 4) == 5.0

def test_pitagoras_cateto():
    assert round(pitagoras(a=3, c=5).valor, 4) == 4.0

def test_eq_render_negativo():
    r = eq_primeiro_grau(3, -15)                 # 3x - 15 = 0
    assert "3x - 15 = 0" in r.passos[0].replace("\\", "")
    assert r.valor == "5"

def test_regra_de_tres():
    assert regra_de_tres(3, 24, 5).valor == 40

def test_catalogo_tem_as_seis():
    assert set(CATALOGO) == {"area_trapezio", "area_triangulo", "area_retangulo",
                             "pitagoras", "eq_primeiro_grau", "regra_de_tres"}
```

- [ ] **Step 2: Rodar — falha** (`.venv/bin/pytest tests/test_calc.py -v`)

- [ ] **Step 3: Implementar `calc.py`**

```python
from __future__ import annotations
import math
from dataclasses import dataclass
from fractions import Fraction

@dataclass
class Resultado:
    valor: float | str | tuple
    passos: list[str]
    unidade: str = ""

def _n(x) -> str:
    xf = float(x)
    return str(int(xf)) if xf == int(xf) else f"{xf:g}"

def area_trapezio(B, b, h) -> Resultado:
    v = (B + b) * h / 2
    return Resultado(v, [
        r"A = \dfrac{(B + b)\cdot h}{2}",
        rf"A = \dfrac{{({_n(B)} + {_n(b)})\cdot {_n(h)}}}{{2}}",
        rf"A = \dfrac{{{_n((B + b) * h)}}}{{2}} = {_n(v)}"], "u²")

def area_triangulo(base, altura) -> Resultado:
    v = base * altura / 2
    return Resultado(v, [r"A = \dfrac{b \cdot h}{2}",
                         rf"A = \dfrac{{{_n(base)} \cdot {_n(altura)}}}{{2}} = {_n(v)}"], "u²")

def area_retangulo(base, altura) -> Resultado:
    return Resultado(base * altura,
                     [rf"A = b \cdot h = {_n(base)} \cdot {_n(altura)} = {_n(base * altura)}"], "u²")

def pitagoras(a=None, b=None, c=None) -> Resultado:
    if c is None:
        c = math.hypot(a, b)
        return Resultado(round(c, 4), [
            r"c^2 = a^2 + b^2",
            rf"c^2 = {_n(a)}^2 + {_n(b)}^2 = {_n(a*a + b*b)}",
            rf"c = \sqrt{{{_n(a*a + b*b)}}} = {c:.4g}"])
    con = a if a is not None else b
    cat = math.sqrt(c * c - con ** 2)
    return Resultado(round(cat, 4), [
        r"cat^2 = c^2 - cat^2",
        rf"cat^2 = {_n(c)}^2 - {_n(con)}^2 = {_n(c*c - con*con)}",
        rf"cat = \sqrt{{{_n(c*c - con*con)}}} = {cat:.4g}"])

def eq_primeiro_grau(a, b) -> Resultado:
    x = Fraction(-b).limit_denominator() / Fraction(a).limit_denominator()
    termo = f"+ {_n(b)}" if b >= 0 else f"- {_n(-b)}"
    return Resultado(_n(x), [
        rf"{_n(a)}x {termo} = 0",
        rf"{_n(a)}x = {_n(-b)}",
        rf"x = \dfrac{{{_n(-b)}}}{{{_n(a)}}} = {_n(x)}"])

def regra_de_tres(a, b, c) -> Resultado:
    x = b * c / a
    return Resultado(round(x, 4), [
        rf"\dfrac{{{_n(a)}}}{{{_n(b)}}} = \dfrac{{{_n(c)}}}{{x}}",
        rf"x = \dfrac{{{_n(b)} \cdot {_n(c)}}}{{{_n(a)}}} = {x:g}"])

CATALOGO = {"area_trapezio": area_trapezio, "area_triangulo": area_triangulo,
            "area_retangulo": area_retangulo, "pitagoras": pitagoras,
            "eq_primeiro_grau": eq_primeiro_grau, "regra_de_tres": regra_de_tres}
```

- [ ] **Step 4: Rodar — passa** (7 testes)

- [ ] **Step 5: Commit** — `git commit -m "calc: 6 funções puras -> Resultado(valor, passos LaTeX)"`

---

## Task 3: figuras/ (lousa + canvas + catalogo)

**Files:**
- Create: `autotuto/figuras/lousa.py`, `autotuto/figuras/canvas.py`, `autotuto/figuras/catalogo.py`, `tests/test_figuras.py`

**Interfaces:**
- Consumes: `config` (cores).
- Produces:
  - `lousa.passo_latex(latex: str) -> bytes` (PNG).
  - `lousa.nova_figura() -> (fig, ax)` (matplotlib, tema lousa, eixos off).
  - `lousa.para_png(fig) -> bytes`.
  - `canvas.figura(spec: dict) -> bytes`. `spec` = `{"pontos": {nome: [x,y]}, "segmentos": [[a,b],...], "poligonos": [{"vs": [nomes], "preenche": bool}], "angulos": [{"vertice": n, "de": n, "para": n}], "rotulos": [{"xy": [x,y], "texto": str}], "marcas": [...]}`. Nomes de ponto resolvem em `pontos`; listas de coords cruas também servem.
  - `catalogo.GERADORES: dict[str, callable]` — `trapezio(**p)`, `triangulo(tipo="reto")`, `retangulo(**p)`, `dois_retangulos(B, b, h)`, `balanca(esq, dir)`, `tabela_prop(**p)`, `reta_numerica(**p)`. Cada um `-> bytes` (chama `canvas.figura`).

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_figuras.py
from PIL import Image
import io
from autotuto.figuras.lousa import passo_latex
from autotuto.figuras.canvas import figura
from autotuto.figuras.catalogo import GERADORES

def _dims(png): 
    return Image.open(io.BytesIO(png)).size

def test_passo_latex_gera_png():
    png = passo_latex(r"A = \dfrac{(B+b)h}{2}")
    assert png[:8] == b"\x89PNG\r\n\x1a\n" and _dims(png)[0] > 100

def test_figura_composicao_minima():
    png = figura({"pontos": {"A": [0, 0], "B": [4, 0], "C": [2, 3]},
                  "poligonos": [{"vs": ["A", "B", "C"], "preenche": True}],
                  "rotulos": [{"xy": [2, -0.5], "texto": "base"}]})
    assert png[:4] == b"\x89PNG"

def test_todos_os_geradores_desenham():
    for nome, fn in GERADORES.items():
        png = fn() if nome not in ("dois_retangulos", "balanca", "tabela_prop") \
              else fn(**{"dois_retangulos": {"B": 18, "b": 10, "h": 6},
                         "balanca": {"esq": "3x", "dir": "15"},
                         "tabela_prop": {"a": 3, "b": 24, "c": 5, "x": 40}}[nome])
        assert png[:4] == b"\x89PNG", nome
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `lousa.py`** — matplotlib Agg, `figure(facecolor=config.COR_FUNDO)`, `ax.set_axis_off()`, `ax.set_aspect("equal")`; `passo_latex` usa `ax.text(0.5, 0.5, f"${latex}$", color=config.COR_GIZ, fontsize=28, ha="center")`. `para_png` → `fig.savefig(buf, format="png", facecolor=fig.get_facecolor(), bbox_inches="tight", dpi=130)`.

- [ ] **Step 4: Implementar `canvas.py`** — `figura(spec)`:
  1. `resolver = lambda n: spec["pontos"][n] if isinstance(n, str) else n`
  2. polígonos: `ax.fill(xs, ys, color=config.COR_AZUL, alpha=0.12 if preenche else 0, edgecolor=config.COR_GIZ, lw=2)`
  3. segmentos: `ax.plot([x1,x2],[y1,y2], color=config.COR_GIZ, lw=2)`
  4. ângulos: arco pequeno (matplotlib `Arc`) — ou quadradinho se ~90°
  5. marcas: ticks de congruência / paralelismo (setas pequenas)
  6. rótulos: `ax.text(x, y, texto, color=config.COR_GIZ, fontsize=15, ha="center")`
  7. `ax.autoscale(); ax.margins(0.15)` → `para_png`

- [ ] **Step 5: Implementar `catalogo.py`** — cada gerador monta um `spec` e chama `figura()`. Ex.:
```python
def trapezio(B=18, b=10, h=6, **_):
    dx = (B - b) / 2
    return figura({"pontos": {"A": [0, 0], "B": [B, 0], "C": [B - dx, h], "D": [dx, h]},
                   "poligonos": [{"vs": ["A", "B", "C", "D"], "preenche": True}],
                   "marcas": [{"tipo": "par", "de": "A", "para": "B"},
                              {"tipo": "par", "de": "D", "para": "C"}],
                   "rotulos": [{"xy": [B/2, -0.7], "texto": f"{B}"},
                               {"xy": [B/2, h+0.4], "texto": f"{b}"},
                               {"xy": [-0.8, h/2], "texto": f"{h}"}]})
```

- [ ] **Step 6: Rodar — passa** · **Step 7: Commit** — `git commit -m "figuras: lousa (tema+LaTeX) + canvas (compositor) + 7 geradores"`

---

## Task 4: aulas.py (4 aulas de ouro)

**Files:**
- Create: `autotuto/aulas.py`, `tests/test_aulas.py`

**Interfaces:**
- Consumes: `schema.Aula`, `schema.validar_estrutura`.
- Produces:
  - `aulas.RAMOS_GENERICOS: dict` — `por_que`, `nao_entendi`, `repete` (cada um `[{"diz": ...}]`).
  - `aulas.carregar(nome: str) -> Aula` (com os genéricos já mergeados; a aula sobrescreve).
  - `aulas.disponiveis() -> list[str]` → `["trapezio", "pitagoras", "eq_primeiro_grau", "regra_de_tres"]`.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_aulas.py
import pytest
from autotuto.aulas import carregar, disponiveis, RAMOS_GENERICOS
from autotuto.schema import validar_estrutura

@pytest.mark.parametrize("nome", ["trapezio", "pitagoras", "eq_primeiro_grau", "regra_de_tres"])
def test_aula_de_ouro_valida(nome):
    a = carregar(nome)
    assert validar_estrutura(a.para_json()) == []

@pytest.mark.parametrize("nome", ["trapezio", "pitagoras", "eq_primeiro_grau", "regra_de_tres"])
def test_tem_ramos_genericos(nome):
    assert {"por_que", "nao_entendi", "repete"} <= set(carregar(nome).ramos)

def test_aula_sobrescreve_generico():
    # pitagoras define seu proprio por_que -> nao pode ser o texto generico
    assert carregar("pitagoras").ramos["por_que"][0]["diz"] != RAMOS_GENERICOS["por_que"][0]["diz"]

def test_trapezio_tem_beat_pergunta_com_fading():
    b = next(x for x in carregar("trapezio").blocos if x.get("pergunta"))
    assert b["pergunta"]["senao"] in carregar("trapezio").ramos
    assert "triangulo" in " ".join(b["pergunta"]["acerta"]).lower()
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `aulas.py`** — portar a pedagogia das 4 aulas do v1 (worked example, `diz_passos`, beat `pergunta` com `acerta`/`confirma`, `diz` narra a decisão). Cada aula é um `dict`. `_GENERICOS` + `_com_genericos(aula)` = `{**_GENERICOS, **aula["ramos"]}`. `_CATALOGO` → `carregar` → `Aula.de_json(_com_genericos(...))`.

  Texto de referência (v1, ainda no `../autotuto-v1-arquivo.bundle` → `git show v2^:professor/aulas.py` ou clone do bundle). Reescrever `diz` do zero, manter a estrutura de beats.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "aulas de ouro: trapezio, pitagoras, eq 1º grau, regra de três + ramos genéricos"`

---

## Task 5: estado.py

**Files:**
- Create: `autotuto/estado.py`, `tests/test_estado.py`

**Interfaces:**
- Consumes: `schema.Aula`.
- Produces:
  - `EstadoAula(aula)`. Métodos: `proximo() -> dict | None`, `entra_ramo(gatilho: str) -> list[dict]` (`[]` se não existe), `drena_ramo()` (generator: rende o resto do ramo e desempilha), `sai_ramo() -> None`, propriedades `na_principal: bool`, `trilha`, `historico: list[str]`, `resumo() -> str`.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_estado.py
from autotuto.schema import Aula
from autotuto.estado import EstadoAula

A = Aula.de_json({"titulo": "T", "blocos": [{"diz": "p1"}, {"diz": "p2"}, {"diz": "p3"}],
                  "ramos": {"por_que": [{"diz": "r1"}, {"diz": "r2"}]}})

def test_percorre_principal():
    e = EstadoAula(A)
    assert [e.proximo()["diz"] for _ in range(3)] == ["p1", "p2", "p3"]
    assert e.proximo() is None

def test_ramo_empilha_drena_retoma():
    e = EstadoAula(A)
    e.proximo()                       # p1
    assert e.entra_ramo("por_que") and not e.na_principal
    assert [b["diz"] for b in e.drena_ramo()] == ["r1", "r2"]
    assert e.na_principal
    assert e.proximo()["diz"] == "p2" # retomou de onde parou

def test_ramo_inexistente_nao_muda_nada():
    e = EstadoAula(A)
    e.proximo()
    assert e.entra_ramo("fantasma") == []
    assert e.na_principal and e.proximo()["diz"] == "p2"

def test_historico_registra_gatilhos():
    e = EstadoAula(A)
    e.proximo(); e.entra_ramo("por_que"); list(e.drena_ramo())
    assert e.historico == ["por_que"]
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `estado.py`** — `Trilha(blocos, i=-1)` com `proximo()`; `EstadoAula._pilha: list[Trilha]` começa com a principal; `entra_ramo` faz `push`; `drena_ramo` `yield` até acabar e dá `pop`; `sai_ramo` `pop` se não-principal; `resumo()` = topico + dados + posição + `historico`.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "estado: EstadoAula (pilha de trilhas, retoma de onde parou)"`

---

## Task 6: classificador.py

**Files:**
- Create: `autotuto/classificador.py`, `tests/test_classificador.py`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `_norm(s) -> str` (NFKD sem acento, minúsculo, sem pontuação, espaço colapsado).
  - `classificar(fala: str, ramos: dict | list) -> str | None`. **Invariante: só devolve gatilho presente em `ramos`, ou None.** "por que" genérico rebaixa pro primeiro `por_que*` da aula.

- [ ] **Step 1: Testes (falhando) — inclui a invariante da honestidade**

```python
# tests/test_classificador.py
from autotuto.classificador import classificar, _norm

RAMOS_TRAP = ["por_que_div_2", "nao_entendi", "e_triangulo", "repete"]
RAMOS_PITA = ["por_que", "nao_entendi", "repete"]

def test_por_que_especifico():
    assert classificar("por que que divide por dois?", RAMOS_TRAP) == "por_que_div_2"

def test_por_que_generico_rebaixa():
    assert classificar("mas por que isso funciona?", RAMOS_TRAP) == "por_que_div_2"
    assert classificar("mas por que isso funciona?", RAMOS_PITA) == "por_que"

def test_repete():
    assert classificar("pode repetir?", RAMOS_PITA) == "repete"
    assert classificar("hã?", RAMOS_PITA) == "repete"

def test_nunca_devolve_gatilho_ausente():
    # "e se fosse um triangulo" casa e_triangulo, mas pitagoras NAO tem esse ramo
    assert classificar("e se fosse um triângulo?", RAMOS_PITA) is None

def test_sem_match_e_none():
    assert classificar("quanto custa o pedreiro", RAMOS_TRAP) is None

def test_norm():
    assert _norm("Pêra Aí, POR QUÊ?!") == "pera ai por que"
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `classificador.py`**

```python
from __future__ import annotations
import re, unicodedata

def _norm(s: str) -> str:
    s = unicodedata.normalize("NFKD", s.lower()).encode("ascii", "ignore").decode()
    return re.sub(r"\s+", " ", re.sub(r"[^a-z0-9 ]", " ", s)).strip()

_REGRAS = [
    ("por_que_div_2", (r"\b(por ?que|pq).*(divid|sobre|metade|media|\bdois\b|\b2\b)",
                       r"(dividir|dividido|divide) por (dois|2)",
                       r"de onde (vem|saiu).*(dois|2)")),
    ("e_triangulo", (r"\b(e se|se fosse|virasse|vira).{0,14}triangulo", r"\btriangulo\b")),
    ("achar_hipotenusa", (r"\bhipotenusa\b", r"lado (maior|comprido)")),
    ("outro_numero", (r"\boutro numero\b", r"e se (fosse|desse) (outro|-?\d)")),
    ("e_se_menos", (r"\b(menos|menor|diminui|cai)\b.*\b(preco|valor|custa)")),
    ("decompor", (r"outr[oa] (jeito|forma)", r"sem (a )?formula", r"nao decorei")),
    ("repete", (r"\b(repete|repetir|repetiu|de novo|outra vez|mais uma vez)\b",
                r"\b(nao ouvi|nao escutei|fala de novo)\b",
                r"^\s*(que|h[aã]n?|como|oi|hein)\s*\??\s*$")),
    ("nao_entendi", (r"nao (entend|peguei|ficou claro|consegui|captei)",
                     r"\b(mais devagar|como assim|me perdi|confus|perdid)",
                     r"explica (melhor|diferente)")),
    ("por_que", (r"\b(por ?que|pq|porqu[eê])\b", r"qual (o|e o) motivo")),
]
_COMPILADAS = [(g, [re.compile(p) for p in ps]) for g, ps in _REGRAS]

def classificar(fala: str, ramos) -> str | None:
    t = _norm(fala)
    disp = list(ramos) if ramos is not None else None
    for gat, res in _COMPILADAS:
        alvo = gat
        if disp is not None and gat not in disp:
            alvo = next((r for r in disp if r.startswith("por_que")), None) if gat == "por_que" else None
            if alvo is None:
                continue
        if any(r.search(t) for r in res):
            return alvo
    return None
```

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "classificador: regex; nunca devolve gatilho ausente da aula"`

---

## Task 7: llm.py

**Files:**
- Create: `autotuto/llm.py`, `tests/test_llm.py`

**Interfaces:**
- Consumes: `config` (`LLM_PROVEDOR`, `LLM_MODELO`, `LLM_CLAUDE`).
- Produces: `llm.perguntar(mensagens: list[dict], *, timeout: float, json_mode: bool = True) -> str`. `mensagens` = `[{"role": "system"|"user"|"assistant", "content": str}]`. Dispatch por `config.LLM_PROVEDOR`. `ConnectionError` se faltar `ANTHROPIC_API_KEY` no modo claude.

- [ ] **Step 1: Testes (falhando) — sem rede, com monkeypatch**

```python
# tests/test_llm.py
import json
from autotuto import llm

def test_dispatch_ollama(monkeypatch):
    capturado = {}
    def fake_urlopen(req, timeout):
        capturado["url"] = req.full_url
        capturado["body"] = json.loads(req.data)
        class R:
            def __enter__(s): return s
            def __exit__(s, *a): pass
            def read(s): return json.dumps({"message": {"content": '{"ok": 1}'}}).encode()
        return R()
    monkeypatch.setattr(llm, "LLM_PROVEDOR", "ollama")
    monkeypatch.setattr(llm.urllib.request, "urlopen", fake_urlopen)
    out = llm.perguntar([{"role": "user", "content": "oi"}], timeout=5)
    assert json.loads(out) == {"ok": 1}
    assert "/api/chat" in capturado["url"] and capturado["body"]["format"] == "json"

def test_claude_sem_chave_erra(monkeypatch):
    monkeypatch.setattr(llm, "LLM_PROVEDOR", "claude")
    monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)
    try:
        llm.perguntar([{"role": "user", "content": "x"}], timeout=5)
        assert False
    except ConnectionError:
        pass
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `llm.py`** — `_ollama(msgs, timeout, json_mode)` → POST `{OLLAMA}/api/chat` com `format:"json"` quando `json_mode`, `options:{temperature:0.2, num_ctx:8192}`; `_claude(msgs, timeout, json_mode)` → POST `api.anthropic.com/v1/messages`, junta `system`, `max_tokens:4096`; `perguntar` faz o dispatch. Tudo `urllib`.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "llm: uma função (perguntar) — Ollama ou Claude via urllib"`

---

## Task 8: cerebro.py

**Files:**
- Create: `autotuto/cerebro.py`, `tests/test_cerebro.py`

**Interfaces:**
- Consumes: `llm.perguntar`, `classificador.classificar`, `config.CEREBRO_TIMEOUT_S`.
- Produces:
  - `cerebro.roteia_interrupcao(fala: str, contexto: str, ramos: dict, *, perguntar=llm.perguntar) -> str | None`. Blindado: exceção/timeout/JSON ruim/ramo inexistente → `None`. `perguntar` injetável pra teste.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_cerebro.py
from autotuto.cerebro import roteia_interrupcao

RAMOS = {"por_que": [{"diz": "..."}], "nao_entendi": [{"diz": "..."}], "repete": [{"diz": "..."}]}

def test_llm_escolhe_ramo():
    fake = lambda msgs, **k: '{"ramo": "nao_entendi"}'
    assert roteia_interrupcao("tô boiando", "ctx", RAMOS, perguntar=fake) == "nao_entendi"

def test_llm_diz_nenhum():
    fake = lambda msgs, **k: '{"ramo": "nenhum"}'
    assert roteia_interrupcao("qual teu nome?", "ctx", RAMOS, perguntar=fake) is None

def test_llm_devolve_ramo_inexistente_vira_none():
    fake = lambda msgs, **k: '{"ramo": "inventado"}'
    assert roteia_interrupcao("x", "ctx", RAMOS, perguntar=fake) is None

def test_llm_explode_vira_none():
    def fake(msgs, **k): raise TimeoutError()
    assert roteia_interrupcao("x", "ctx", RAMOS, perguntar=fake) is None

def test_lixo_de_json_vira_none():
    fake = lambda msgs, **k: "desculpa não entendi"
    assert roteia_interrupcao("x", "ctx", RAMOS, perguntar=fake) is None
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `cerebro.py`**

```python
from __future__ import annotations
import json
from autotuto.config import CEREBRO_TIMEOUT_S
from autotuto import llm as _llm

_SIS = ('Você roteia a dúvida de um aluno que INTERROMPEU uma aula para UM tópico já '
        'preparado pelo professor. Se nenhum resolve, responda "nenhum" — não invente. '
        'Responda só: {"ramo": "<nome exato da lista>"} ou {"ramo": "nenhum"}.')

def _catalogo(ramos: dict) -> str:
    return "\n".join(f"- {n}: {(b[0].get('diz','') if b else '')[:80]}" for n, b in ramos.items())

def roteia_interrupcao(fala, contexto, ramos, *, perguntar=_llm.perguntar) -> str | None:
    usr = (f"O professor vinha dizendo:\n{contexto or '(começo)'}\n\n"
           f'O aluno interrompeu: "{fala}"\n\nTópicos preparados:\n{_catalogo(ramos)}')
    try:
        txt = perguntar([{"role": "system", "content": _SIS},
                         {"role": "user", "content": usr}], timeout=CEREBRO_TIMEOUT_S)
        ini, fim = txt.find("{"), txt.rfind("}")
        ramo = json.loads(txt[ini:fim + 1]).get("ramo", "nenhum") if ini >= 0 else "nenhum"
    except Exception:
        return None
    return ramo if ramo in ramos else None
```

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "cerebro: LLM curto da interrupção (blindado, injetável)"`

---

## Task 9: validador.py

**Files:**
- Create: `autotuto/validador.py`, `tests/test_validador.py`

**Interfaces:**
- Consumes: `calc.CATALOGO`, `figuras.catalogo.GERADORES`.
- Produces: `validador.checar_matematica(aula: dict) -> list[str]` — avisos (não-fatais): `calc`/`figura` com `gerador` desconhecido, params que fazem a função levantar exceção, área/comprimento negativo.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_validador.py
from autotuto.validador import checar_matematica

def test_gerador_de_calc_desconhecido():
    a = {"blocos": [{"diz": "x", "calc": {"gerador": "fantasma", "params": {}}}], "ramos": {}}
    assert any("fantasma" in e for e in checar_matematica(a))

def test_params_que_explodem():
    a = {"blocos": [{"diz": "x", "calc": {"gerador": "regra_de_tres", "params": {"a": 0, "b": 1, "c": 1}}}],
         "ramos": {}}
    assert checar_matematica(a)   # divisão por zero -> aviso

def test_aula_boa_sem_avisos():
    a = {"blocos": [{"diz": "x", "calc": {"gerador": "area_trapezio",
         "params": {"B": 18, "b": 10, "h": 6}}}], "ramos": {}}
    assert checar_matematica(a) == []
```

- [ ] **Step 2: Rodar — falha** · **Step 3: Implementar** (itera blocos + ramos; `try: fn(**params) except Exception as e: avisos.append(...)`; checa `valor < 0` pra áreas) · **Step 4: passa** · **Step 5: Commit** — `git commit -m "validador: checagem matemática (avisos não-fatais)"`

---

## Task 10: planejador.py

**Files:**
- Create: `autotuto/planejador.py`, `tests/test_planejador.py`

**Interfaces:**
- Consumes: `llm.perguntar`, `schema`, `validador.checar_matematica`, `aulas`.
- Produces:
  - `planejador.Relatorio` (dataclass: `ok: bool`, `erros: list[str]`, `avisos: list[str]`).
  - `planejador.planeja(problema: str, *, tentativas=3, perguntar=llm.perguntar) -> tuple[Aula, Relatorio]`. Few-shot dirigido: casa palavra→tópico (`_PISTAS`), injeta a aula de ouro correspondente enxuta. Loop de correção: se `validar_estrutura` acha erro, devolve pro LLM. Sem LLM disponível → `carregar("trapezio")` + `Relatorio(ok=False, ...)`.
  - `planejador._exemplo_dirigido(problema) -> str | None` (JSON da aula de ouro, `blocos[:4]`).

- [ ] **Step 1: Testes (falhando) — LLM fake**

```python
# tests/test_planejador.py
import json
from autotuto.planejador import planeja, _exemplo_dirigido
from autotuto.aulas import carregar

def test_pista_de_equacao():
    assert json.loads(_exemplo_dirigido("resolva 2x - 8 = 0"))["topico"] == "eq_primeiro_grau"
    assert json.loads(_exemplo_dirigido("x + 5 = 12"))["topico"] == "eq_primeiro_grau"

def test_pista_regra_de_tres():
    assert json.loads(_exemplo_dirigido("se 3 cadernos custam 24, quanto custam 5"))["topico"] == "regra_de_tres"

def test_sem_pista_e_none():
    assert _exemplo_dirigido("me explica o universo") is None

def test_planeja_valida_e_corrige():
    ruim = json.dumps({"titulo": "x", "blocos": [{}], "ramos": {}})           # bloco vazio
    bom = json.dumps(carregar("trapezio").para_json())
    respostas = iter([ruim, bom])
    aula, rel = planeja("área de um trapézio", perguntar=lambda m, **k: next(respostas))
    assert rel.ok and aula.blocos

def test_sem_llm_cai_no_trapezio():
    def morto(m, **k): raise ConnectionError()
    aula, rel = planeja("qualquer coisa", perguntar=morto)
    assert aula.titulo == carregar("trapezio").titulo and not rel.ok
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `planejador.py`** — `_PISTAS` (regex por tópico, incl. `r"\b\d*x\s*[-+=]"` pra equação), `_exemplo_dirigido` itera as pistas, `planeja` monta as mensagens (system = regras do SPEC §6 + catálogo de geradores; user = exemplo dirigido + problema), roda o loop de `tentativas`, valida com `validar_estrutura` + `checar_matematica`.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "planejador: problema -> Aula (few-shot dirigido + loop de validação)"`

---

## Task 11: tocador.py

**Files:**
- Create: `autotuto/tocador.py`, `tests/test_tocador.py`

**Interfaces:**
- Consumes: `estado.EstadoAula`, `classificador.classificar`, `cerebro.roteia_interrupcao`, `figuras.catalogo.GERADORES`, `figuras.lousa.passo_latex`, `calc.CATALOGO`, `config` (`SETTLE_S`, `PAUSA`).
- Produces:
  - `Tocador(falar=None, ouvir=None, desenhar=None, *, cerebro=cerebro.roteia_interrupcao, pausas=True)`.
    - `falar(txt) -> str | None` (None = seguiu; str = aluno interrompeu com isso).
    - `ouvir(seg) -> str | None`.
    - `desenhar(png: bytes, rotulo: str) -> None`.
  - `Tocador.toca(aula: Aula, *, interrupcoes: dict[int, str] | None = None, respostas: dict[int, str] | None = None) -> EstadoAula`.
  - Constante `HONESTO = "Essa eu não preparei agora — sigo daqui, e a gente volta nisso."`

- [ ] **Step 1: Testes (falhando) — o coração**

```python
# tests/test_tocador.py
from autotuto.aulas import carregar
from autotuto.tocador import Tocador, HONESTO

def cap():
    L = []
    def falar(t, _barge=[0], _quando=[None], _txt=[None]):
        L.append(t)
        return None
    falar.L = L
    return falar

def roda(nome, *, barge=None, quando=2, resp=None, cerebro=None):
    L = []
    n = [0]
    def falar(t):
        L.append(t); n[0] += 1
        return barge if (barge and n[0] == quando) else None
    est = Tocador(falar=falar, ouvir=lambda s: resp, pausas=False, cerebro=cerebro).toca(carregar(nome))
    return est, L

def test_toca_a_aula_inteira():
    est, L = roda("pitagoras")
    assert "pitagoras" in est.resumo() and len(L) >= 4

def test_interrupcao_classificada_entra_no_ramo():
    est, L = roda("pitagoras", barge="mas por que isso funciona?")
    assert est.historico == ["por_que"]

def test_interrupcao_fora_do_script_e_honesta_e_retoma():
    est, L = roda("pitagoras", barge="quanto custa o pedreiro", cerebro=None)
    assert any(HONESTO in x for x in L)
    assert est.historico == []                       # NÃO entrou em ramo nenhum
    assert "deixa eu achar um jeito melhor" not in " ".join(L).lower()

def test_camada_2_roteia_quando_regex_falha():
    fake = lambda fala, ctx, ramos: "nao_entendi" if "boiando" in fala else None
    est, L = roda("pitagoras", barge="tô boiando total", cerebro=fake)
    assert est.historico == ["nao_entendi"]

def test_cerebro_devolvendo_lixo_e_ignorado():
    est, L = roda("pitagoras", barge="xyz", cerebro=lambda *a: "ramo_fantasma")
    assert est.historico == [] and any(HONESTO in x for x in L)

def test_pergunta_nao_sei_acolhe_e_vai_pro_senao():
    est, L = roda("trapezio", resp="sei lá, não faço ideia")
    assert any("não saber" in x.lower() for x in L)
    b = next(x for x in carregar("trapezio").blocos if x.get("pergunta"))
    assert est.historico == [b["pergunta"]["senao"]]

def test_pergunta_resposta_certa_faz_fading():
    est, L = roda("trapezio", resp="acho que vira um triângulo")
    assert est.historico == []                       # pulou a derivação, sem ramo

def test_modo_gravacao_scriptado():
    est = Tocador(pausas=False, cerebro=None).toca(
        carregar("trapezio"), interrupcoes={2: "por_que_div_2"}, respostas={4: "vira um triângulo"})
    assert "por_que_div_2" in est.historico
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `tocador.py`** seguindo o pseudocódigo do SPEC §7:
  - `_toca_bloco(bloco)`: figura → (`sleep(SETTLE_S)` se `pausas`) → `diz` (append em `self._falas`, mantém 3) → se `falar` devolveu str: `("resposta", str)` se o bloco é `pergunta`, senão `("barge", str)` → `pergunta`: `("resposta", self.ouvir(escuta_s))` (ou `_resp_scriptada`) → `calc`: desenha `passo_latex` de cada passo, narra `diz_passos[i]`.
  - `toca()`: `est.aula.ramos = {**RAMOS_GENERICOS, **est.aula.ramos}` (import de `aulas`); loop `while est.proximo()`; `_resp_scriptada` via `respostas`; `interrupcoes` via `script.pop(n_princ)`.
  - `_resolve_interrupcao(fala, est)`: `classificar` → (se None e `self.cerebro`) `self.cerebro(fala, " / ".join(self._falas), est.aula.ramos)` → `x if x in ramos else None`.
  - barge com `gat` None → `self.falar(HONESTO)`, sem tocar estado.
  - `pergunta`: `_NAO_SABE` regex → acolhe + `gat = senao`; senão lógica de `acerta`/`eco`/`confirma` (fading) → `gat = achou or senao`.
  - `_entra_ramo(est, gat, filler)`: fala um filler curto (banco pequeno inline), `est.entra_ramo`, `for b in est.drena_ramo(): _toca_bloco(b)`, fala "Voltando de onde a gente parou."

- [ ] **Step 4: Rodar — passa** (9 testes) · **Step 5: Commit** — `git commit -m "tocador: o loop + interrupção 3 camadas + fallback honesto + fading"`

---

## Task 12: visor.py

**Files:**
- Create: `autotuto/visor.py`, `tests/test_visor.py`

**Interfaces:**
- Consumes: `config` (`VISOR_PORTA`, `VISOR_POLL_MS`, `RITMO_S_POR_CHAR`, cores).
- Produces:
  - `Visor(porta=config.VISOR_PORTA, ritmo=config.RITMO_S_POR_CHAR).start() -> Visor`.
  - `visor.falar(txt) -> str | None` (dorme `len*ritmo`, devolve injeção de tecla se houver).
  - `visor.desenhar(png, rot) -> None`, `visor.estado(nome)`, `visor.mostrar_fala(txt)`, `visor.aluno(txt)`, `visor.resumo(txt)`.
  - `visor.pop_injecao() -> str | None` (teclas 1/2/3/0), `visor.pop_pergunta() -> str | None` (caixa de texto).
  - HTTP: `GET /`, `GET /estado`, `GET /frame.png`, `POST /interromper`, `POST /perguntar`. Engole `BrokenPipeError`/`ConnectionResetError`.

- [ ] **Step 1: Testes (falhando)**

```python
# tests/test_visor.py
import json, time, threading, urllib.request
from autotuto.visor import Visor

def _post(porta, rota, texto):
    urllib.request.urlopen(urllib.request.Request(
        f"http://127.0.0.1:{porta}{rota}", data=json.dumps({"texto": texto}).encode(),
        headers={"Content-Type": "application/json"}, method="POST"))

def test_estado_e_frame():
    v = Visor(porta=8123, ritmo=0.001).start(); time.sleep(0.2)
    v.desenhar(b"\x89PNG_fake", "x"); v.mostrar_fala("olá")
    s = json.loads(urllib.request.urlopen("http://127.0.0.1:8123/estado").read())
    assert s["professor"] == "olá" and s["frame"] == 1
    v.stop()

def test_tecla_vira_injecao_e_falar_devolve():
    v = Visor(porta=8124, ritmo=0.05).start(); time.sleep(0.2)
    res = {}
    def fala_longa(): res["r"] = v.falar("uma frase bem longa " * 5)
    th = threading.Thread(target=fala_longa); th.start()
    time.sleep(0.3); _post(8124, "/interromper", "não entendi")
    th.join(3)
    assert res["r"] == "não entendi"
    v.stop()

def test_caixa_de_texto_vira_pergunta():
    v = Visor(porta=8125, ritmo=0.001).start(); time.sleep(0.2)
    _post(8125, "/perguntar", "me explica trapézio")
    assert v.pop_pergunta() == "me explica trapézio"
    v.stop()

def test_html_keydown_ignora_input():
    v = Visor(porta=8126, ritmo=0.001).start(); time.sleep(0.2)
    html = urllib.request.urlopen("http://127.0.0.1:8126/").read().decode()
    assert 'e.target.tagName === "INPUT"' in html
    v.stop()
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `visor.py`** — `ThreadingHTTPServer`, handler com `handle_one_request` engolindo `BrokenPipeError`/`ConnectionResetError`, `log_message` mudo. Página: tema lousa das cores de `config`, polling `/estado` a cada `VISOR_POLL_MS`, `<img>` troca quando `frame` muda, `const TECLAS = {"1":"por que que divide por dois?","2":"não entendi essa parte","3":"e se fosse um triângulo?","0":"acho que vira um triângulo"}`, keydown com guarda `if (e.target.tagName === "INPUT" || e.target.tagName === "TEXTAREA") return;`, `<form>` com `<input>` → `POST /perguntar`.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "visor: tela lousa (http.server), teclado como 1º caminho, engole BrokenPipe"`

---

## Task 13: voz.py

**Files:**
- Create: `autotuto/voz.py`, `tests/test_voz.py`

**Interfaces:**
- Consumes: `config` (`STT_MODELO`, `STT_DEVICE`, `FALA_TIMEOUT_S`, `GRAVA_RESTO_S`, `SILENCIO_MS`, `BARGE_IN`, `TTS_LENGTH_SCALE`), `piper`, `faster_whisper`.
- Produces:
  - `Voz(modelo_stt=config.STT_MODELO)`. `__init__` carrega Piper + faster-whisper e **pré-aquece** (transcreve 0,5 s de silêncio, sintetiza "um dois três").
  - `voz.falar(txt) -> str | None` — Piper num thread com cão-de-guarda `FALA_TIMEOUT_S`; se `BARGE_IN`, um monitor de VAD corta e grava mais `GRAVA_RESTO_S`, transcreve; injeção de teclado (`on_injecao`) tem prioridade.
  - `voz.ouvir(seg) -> str | None` — grava até `seg` (ou até `SILENCIO_MS` de silêncio pós-fala), transcreve. `on_injecao` primeiro.
  - `voz.on_injecao: Callable[[], str | None] | None`, `voz.on_inicio_fala: Callable[[str], None] | None`.

- [ ] **Step 1: Teste (falhando) — sem áudio real, componentes fake**

```python
# tests/test_voz.py
from autotuto import voz as _voz

class _PiperFake:
    rate = 22050
    def synth(self, t): return b"\x00\x00" * 100

class _STTFake:
    def transcribe(self, audio): 
        class R: text = "resposta transcrita"
        return R()

def test_falar_sem_barge_devolve_none(monkeypatch):
    monkeypatch.setattr(_voz, "BARGE_IN", False)
    v = _voz.Voz.__new__(_voz.Voz)
    v.piper, v.stt, v.on_injecao, v.on_inicio_fala = _PiperFake(), _STTFake(), None, None
    monkeypatch.setattr(_voz, "_tocar_pcm", lambda pcm, rate: None)
    assert v.falar("olá turma") is None

def test_injecao_de_teclado_tem_prioridade(monkeypatch):
    v = _voz.Voz.__new__(_voz.Voz)
    v.piper, v.stt = _PiperFake(), _STTFake()
    v.on_injecao = lambda: "tecla 2"
    v.on_inicio_fala = None
    monkeypatch.setattr(_voz, "_tocar_pcm", lambda pcm, rate: None)
    assert v.falar("qualquer coisa") == "tecla 2"
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar `voz.py`** — SEM jarvis. `piper.PiperVoice.load(...)` direto; `faster_whisper.WhisperModel(config.STT_MODELO, device=config.STT_DEVICE, compute_type="int8")`. `_tocar_pcm(pcm, rate)` via `subprocess` (`pw-play`/`aplay`) ou `sounddevice` se estiver nas deps. `_gravar(seg)` via `arecord -D default -f S16_LE -r 16000 -c 1` num `Popen`, lê frames, VAD simples (energia RMS) pra achar início/fim de fala, corta em `SILENCIO_MS`. `falar`: thread do Piper + poll de `on_injecao` + cão-de-guarda.

  **Nota:** o único ponto que toca hardware. Mantém DENTRO deste arquivo — nada de VAD/monitor/config em módulo separado. Se der ruim no prazo, `BARGE_IN=0` + `on_injecao` (teclado) já entrega a demo.

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit** — `git commit -m "voz: adapter fino Piper + faster-whisper (zero jarvis, pré-aquece)"`

---

## Task 14: demos/ + roteiros/

**Files:**
- Create: `demos/demo_texto.py`, `demos/demo_voz.py`, `demos/demo_roteiro.py`, `roteiros/trapezio.json`
- Test: `tests/test_demos.py` (só smoke: importam sem erro + `demo_roteiro` roda headless)

**Interfaces:**
- Consumes: `Visor`, `Tocador`, `Voz`, `planejador`, `aulas`.
- Produces: 3 entry points executáveis.

- [ ] **Step 1: Teste (falhando)**

```python
# tests/test_demos.py
import json, importlib

def test_demos_importam():
    for m in ("demos.demo_texto", "demos.demo_voz", "demos.demo_roteiro"):
        importlib.import_module(m)

def test_roteiro_headless(tmp_path):
    from autotuto.aulas import carregar
    from autotuto.tocador import Tocador
    r = json.load(open("roteiros/trapezio.json"))
    est = Tocador(pausas=False, cerebro=None).toca(
        carregar(r["aula"]),
        interrupcoes={int(k): v for k, v in r["interrupcoes"].items()},
        respostas={int(k): v for k, v in r["respostas"].items()})
    assert est.historico   # disparou pelo menos a interrupção scriptada
```

- [ ] **Step 2: Rodar — falha**

- [ ] **Step 3: Implementar os 3 demos**
  - `demo_texto.py`: `Visor` + `Tocador(falar=visor.falar, ouvir=<poll pop_injecao>, desenhar=visor.desenhar)`; loop lê `visor.pop_pergunta()` → `planejador.planeja()` → `tocador.toca()`.
  - `demo_voz.py`: `os.environ.setdefault("AUTOTUTO_BARGE_IN", "0")`; `Voz()` + `Visor()`; liga `voz.on_injecao = visor.pop_injecao`, `voz.on_inicio_fala = visor.mostrar_fala`; `Tocador(falar=voz.falar, ouvir=voz.ouvir, desenhar=visor.desenhar)`.
  - `demo_roteiro.py`: lê `sys.argv[1]` (JSON), `Visor` + `Tocador(cerebro=None)`, `toca(carregar(r["aula"]), interrupcoes=..., respostas=...)`.
  - `roteiros/trapezio.json`: `{"aula":"trapezio","ritmo":0.06,"espera":4,"interrupcoes":{"2":"por_que_div_2"},"respostas":{"4":"acho que vira um triângulo"}}`

- [ ] **Step 4: Rodar — passa** · **Step 5: Commit + push** — `git commit -m "demos: texto, voz, roteiro (modo gravação) + roteiros/trapezio.json"` && `git push origin main`

---

## Self-Review

**1. Spec coverage:**
- §2 regra única (nunca mentir) → Task 11 (`test_interrupcao_fora_do_script_e_honesta_e_retoma`) + Task 6 (`test_nunca_devolve_gatilho_ausente`). ✔
- §3 beats+ramos+estado → Tasks 1, 5. ✔ · LLM só planeja → Tasks 7, 10 (nunca chama calc/figuras). ✔ · aulas de ouro → Task 4. ✔ · 3 camadas → Task 11. ✔ · ramos genéricos → Task 4 + Task 11 (merge). ✔ · teclado principal → Task 12 + Task 14 (`BARGE_IN=0`). ✔ · figuras procedurais → Task 3. ✔ · cálculo em Python → Task 2. ✔ · modo gravação → Task 14. ✔
- §4 anti-Frankenstein: sem jarvis → Task 13 (nota explícita). ✔ · sem Dual LLM → Task 8 (uma chamada). ✔ · ~8 figuras → Task 3 (7 geradores). ✔ · timings em config → Task 1 + Global Constraints. ✔ · classificador honesto → Task 6. ✔ · validador em 2 arquivos → Tasks 1 (estrutura) + 9 (matemática). ✔
- §5 direção de dependência → seção File Structure (lista de imports permitidos). ✔
- §6 modelo de dados → Task 1. ✔
- §7 loop de interrupção → Task 11 (pseudocódigo copiado). ✔
- §8 config → Task 1. ✔
- §9 constraints → Global Constraints. ✔
- §10 vídeo → Task 14 (`roteiros/trapezio.json` cobre os 4 momentos). ✔
- §11 ordem → Tasks 0–14 na ordem. ✔

**2. Placeholder scan:** Task 3 steps 3-5 e Task 13 step 3 descrevem implementação em prosa densa em vez de código completo (matplotlib e áudio são verbosos). Aceitável: as interfaces (assinaturas, tipos de retorno, formato do `spec`) estão 100% especificadas; o corpo é mecânico. Todo teste tem código real. Nenhum "TODO"/"TBD".

**3. Type consistency:**
- `Resultado(valor, passos, unidade)` — consistente em Tasks 2, 9, 11. ✔
- `classificar(fala, ramos) -> str | None` — Tasks 6, 8, 11. ✔
- `roteia_interrupcao(fala, contexto, ramos, *, perguntar=)` — Tasks 8, 11. ✔
- `perguntar(mensagens, *, timeout, json_mode=True) -> str` — Tasks 7, 8, 10. ✔
- `EstadoAula`: `proximo`/`entra_ramo`/`drena_ramo`/`sai_ramo`/`na_principal`/`historico`/`resumo` — Tasks 5, 11. ✔
- `Tocador(falar, ouvir, desenhar, *, cerebro, pausas)` + `toca(aula, *, interrupcoes, respostas)` — Tasks 11, 14. ✔
- `Visor`: `falar`/`ouvir`(via poll)/`desenhar`/`estado`/`mostrar_fala`/`aluno`/`resumo`/`pop_injecao`/`pop_pergunta` — Tasks 12, 14. ✔
- `figuras.catalogo.GERADORES` / `calc.CATALOGO` — Tasks 2, 3, 9, 11. ✔

Sem lacunas encontradas.
